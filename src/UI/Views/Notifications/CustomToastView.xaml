<UserControl x:Class="PakMaster.UI.Views.CustomToastView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:notifications="clr-namespace:Notification.Wpf.Controls;assembly=Notification.Wpf"
             xmlns:converters="clr-namespace:PakMaster.UI.Converters"
             Width="320">
    
    <UserControl.Resources>
        <converters:ToastTypeToBrushConverter x:Key="ToastTypeToBrushConverter" />
        <converters:ToastTypeToIconConverter x:Key="ToastTypeToIconConverter" />
    </UserControl.Resources>

    <Border Background="#252525" CornerRadius="0,8,8,0" BorderThickness="6,0,0,0"
            BorderBrush="{Binding Type, Converter={StaticResource ToastTypeToBrushConverter}}"
            Padding="12,12,16,12" Margin="5">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="8" ShadowDepth="2" />
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Icon -->
            <iconPacks:PackIconMaterial Kind="{Binding Type, Converter={StaticResource ToastTypeToIconConverter}}"
                                        Foreground="{Binding Type, Converter={StaticResource ToastTypeToBrushConverter}}"
                                        Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,15,0" />

            <!-- Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,8,0">
                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="White" FontSize="14" Margin="0,0,0,4" TextWrapping="Wrap" />
                <TextBlock Text="{Binding Message}" Foreground="#D0D0D0" FontSize="12" TextWrapping="Wrap" />
            </StackPanel>

            <!-- Close Button -->
            <Button Grid.Column="2" VerticalAlignment="Top" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0"
                    notifications:Notification.CloseOnClick="True" Margin="0,-4,-4,0" Cursor="Hand" ToolTip="Close">
                <iconPacks:PackIconMaterial Kind="Close" Foreground="#999999" Width="12" Height="12" />
            </Button>
        </Grid>
    </Border>
</UserControl>