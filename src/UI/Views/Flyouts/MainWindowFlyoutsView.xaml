<mah:FlyoutsControl x:Class="PakMaster.UI.Views.Flyouts.MainWindowFlyoutsView"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                    xmlns:constants="clr-namespace:PakMaster.Core.Constants"
                    xmlns:viewmodels="clr-namespace:PakMaster.UI.State"
                    xmlns:lex="http://wpflocalizeextension.codeplex.com"
                    lex:LocalizeDictionary.DesignCulture="en"
                    lex:ResxLocalizationProvider.DefaultAssembly="PakMaster"
                    lex:ResxLocalizationProvider.DefaultDictionary="PakMaster.UI.Resources.Resources"
                    xmlns:extensions="clr-namespace:PakMaster.Core.Extensions"
                    FlowDirection="{extensions:FlowDirection}"
                    d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowState}"
                    mc:Ignorable="d">

    <mah:FlyoutsControl.Resources>
        <Style x:Key="CenteredCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </mah:FlyoutsControl.Resources>
    <!-- ============ App Settings Flyout START ============ -->
    <mah:Flyout x:Name="AppSettingsFlyout"
                Header="{lex:Loc AppSettings_Title}"
                Position="Left"
                Width="{lex:Loc Num_AppSettings_Flyout_Width}"
                Foreground="{DynamicResource MahApps.Brushes.Flyout.Foreground}"
                Background="{DynamicResource Alr.Brushes.Flyout.Background}"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                BorderThickness="0 0 1 0"
                IsOpen="{Binding IsAppSettingsFlyoutOpen}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <Grid Margin="20,10,20,20">
                    <Grid.RowDefinitions>
                        <!-- 0 -->
                        <RowDefinition Height="Auto" />
                        <!-- 1 -->
                        <RowDefinition Height="Auto" />
                        <!-- 2 -->
                        <RowDefinition Height="Auto" />
                        <!-- 3 -->
                        <RowDefinition Height="Auto" />
                        <!-- 4 -->
                        <RowDefinition Height="Auto" />
                        <!-- 5 -->
                        <RowDefinition Height="Auto" />
                        <!-- 6 -->
                        <RowDefinition Height="Auto" />
                        <!-- 7 -->
                        <RowDefinition Height="Auto" />
                        <!-- 8 -->
                        <RowDefinition Height="Auto" />
                        <!-- 9 -->
                        <RowDefinition Height="Auto" />
                        <!-- 10 -->
                        <RowDefinition Height="Auto" />
                        <!-- 11 -->
                        <RowDefinition Height="Auto" />
                        <!-- 12 -->
                        <RowDefinition Height="Auto" />
                        <!-- 13 -->
                        <RowDefinition Height="Auto" />
                        <!-- 14 -->
                        <RowDefinition Height="Auto" />
                        <!-- 15 -->
                        <RowDefinition Height="Auto" />
                        <!-- 16 -->
                        <RowDefinition Height="Auto" />
                        <!-- 17 -->
                        <RowDefinition Height="Auto" />
                        <!-- 18 -->
                        <RowDefinition Height="Auto" />
                        <!-- 19 -->
                        <RowDefinition Height="Auto" />
                        <!-- 20 -->
                        <RowDefinition Height="Auto" />
                        <!-- 21 -->
                        <RowDefinition Height="Auto" />
                        <!-- 22 -->
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Appearance Section -->
                    <Separator Grid.Row="0" Background="{StaticResource MahApps.Brushes.Gray.SemiTransparent}" Margin="0,10,0,20" />
                    <TextBlock Grid.Row="1" Text="{lex:Loc AppSettings_Section_Appearance}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15" />

                    <!--<Grid Grid.Row="0" Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                                    <TextBlock Text="App theme" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                                    <TextBlock Text="Select your preference between a light or dark presentation scheme." Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,50,0" />
                                </StackPanel>

                                <ComboBox x:Name="BaseThemeDropdown"
                                          Grid.Column="1"
                                          Width="150"
                                          Height="35"
                                          mah:ControlsHelper.CornerRadius="2"
                                          Background="{DynamicResource MahApps.Brushes.Transparent}"
                                          BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
                                          Foreground="{DynamicResource MahApps.Brushes.Gray6}"
                                          VerticalAlignment="Center"
                                          BorderThickness="1"
                                          VerticalContentAlignment="Center"
                                          SelectionChanged="BaseThemeDropdown_SelectionChanged"
                                          mah:TextBoxHelper.Watermark="Select Theme...">

                                    <ComboBox.Resources>
                                        <SolidColorBrush x:Key="MahApps.Brushes.ComboBox.Border.MouseOver" Color="{DynamicResource MahApps.Colors.Gray5}" />
                                        <SolidColorBrush x:Key="MahApps.Brushes.ComboBox.Border.Focus" Color="{DynamicResource MahApps.Colors.Gray7}" />
                                        <SolidColorBrush x:Key="MahApps.Brushes.ComboBox.PopupBorder" Color="{DynamicResource MahApps.Colors.Gray7}" />
                                        <SolidColorBrush x:Key="MahApps.Brushes.Gray1" Color="{StaticResource MahApps.Colors.Gray}" />
                                        <SolidColorBrush x:Key="MahApps.Brushes.Gray5" Color="{StaticResource MahApps.Colors.Gray.SemiTransparent}" />
                                    </ComboBox.Resources>

                                    <ComboBox.Style>
                                        <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                                            <Setter Property="Background" Value="Transparent" />
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray.SemiTransparent}" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ComboBox.Style>

                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" FontSize="12" VerticalAlignment="Center" Margin="5,0,0,0" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </Grid>-->

                    <!-- Language Dropdown -->
                    <Grid Grid.Row="2" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                            <TextBlock Text="{lex:Loc AppSettings_Lbl_Language}" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                            <TextBlock Text="{lex:Loc AppSettings_Desc_Language}" Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,50,0" />
                        </StackPanel>

                        <ComboBox x:Name="LanguageDropdown"
                                  Grid.Column="1"
                                  Width="150"
                                  Height="35"
                                  mah:ControlsHelper.CornerRadius="2"
                                  Background="{DynamicResource MahApps.Brushes.Transparent}"
                                  BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
                                  Foreground="{DynamicResource MahApps.Brushes.Gray6}"
                                  VerticalAlignment="Center"
                                  BorderThickness="1"
                                  VerticalContentAlignment="Center"
                                  ItemsSource="{Binding SupportedLanguages, RelativeSource={RelativeSource AncestorType=Window}}"
                                  DisplayMemberPath="DisplayName"
                                  SelectionChanged="LanguageDropdown_SelectionChanged"
                                  mah:TextBoxHelper.Watermark="Select Language...">

                            <ComboBox.Resources>
                                <SolidColorBrush x:Key="MahApps.Brushes.ComboBox.Border.MouseOver" Color="{DynamicResource MahApps.Colors.Gray5}" />
                                <SolidColorBrush x:Key="MahApps.Brushes.ComboBox.Border.Focus" Color="{DynamicResource MahApps.Colors.Gray7}" />
                                <SolidColorBrush x:Key="MahApps.Brushes.ComboBox.PopupBorder" Color="{DynamicResource MahApps.Colors.Gray7}" />
                                <SolidColorBrush x:Key="MahApps.Brushes.Gray1" Color="{StaticResource MahApps.Colors.Gray}" />
                                <SolidColorBrush x:Key="MahApps.Brushes.Gray5" Color="{StaticResource MahApps.Colors.Gray.SemiTransparent}" />
                            </ComboBox.Resources>
                        </ComboBox>
                    </Grid>

                    <!-- Accent Color Dropdown -->
                    <Grid Grid.Row="3" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                            <TextBlock Text="{lex:Loc AppSettings_Lbl_AccentColor}" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                            <TextBlock Text="{lex:Loc AppSettings_Desc_AccentColor}" Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,50,0" />
                        </StackPanel>

                        <ComboBox x:Name="AccentColorDropdown"
                                  Grid.Column="1"
                                  Width="150"
                                  Height="35"
                                  mah:ControlsHelper.CornerRadius="2"
                                  Background="{DynamicResource MahApps.Brushes.Transparent}"
                                  BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
                                  Foreground="{DynamicResource MahApps.Brushes.Gray6}"
                                  VerticalAlignment="Center"
                                  BorderThickness="1"
                                  VerticalContentAlignment="Center"
                                  SelectionChanged="AccentColorDropdown_SelectionChanged"
                                  mah:TextBoxHelper.Watermark="Select Color...">

                            <ComboBox.Resources>
                                <SolidColorBrush x:Key="MahApps.Brushes.ComboBox.Border.MouseOver" Color="{DynamicResource MahApps.Colors.Gray5}" />
                                <SolidColorBrush x:Key="MahApps.Brushes.ComboBox.Border.Focus" Color="{DynamicResource MahApps.Colors.Gray7}" />
                                <SolidColorBrush x:Key="MahApps.Brushes.ComboBox.PopupBorder" Color="{DynamicResource MahApps.Colors.Gray7}" />
                                <SolidColorBrush x:Key="MahApps.Brushes.Gray1" Color="{StaticResource MahApps.Colors.Gray}" />
                                <SolidColorBrush x:Key="MahApps.Brushes.Gray5" Color="{StaticResource MahApps.Colors.Gray.SemiTransparent}" />
                            </ComboBox.Resources>

                            <ComboBox.Style>
                                <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray.SemiTransparent}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Style>

                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                        <Ellipse Width="12"
                                                 Height="12"
                                                 Fill="{Binding ShowcaseBrush}"
                                                 Stroke="{DynamicResource MahApps.Brushes.ThemeForeground}"
                                                 StrokeThickness="0.5"
                                                 VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding ColorScheme}" FontSize="12" Margin="8,0,0,0" VerticalAlignment="Center" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </Grid>

                    <!-- Interface Scale Slider -->
                    <Grid Grid.Row="4" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                            <TextBlock Text="{lex:Loc AppSettings_Lbl_InterfaceScale}" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                            <TextBlock Text="{lex:Loc AppSettings_Desc_InterfaceScale}" Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,50,0" />
                        </StackPanel>

                        <Slider x:Name="InterfaceScaleSlider"
                                x:FieldModifier="public"
                                Grid.Column="1"
                                Width="150"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"

                                Minimum="75"
                                Maximum="150"
                                Value="100"

                                TickFrequency="5"
                                IsSnapToTickEnabled="False"
                                TickPlacement="TopLeft"

                                AutoToolTipPlacement="TopLeft"

                                SmallChange="1"
                                LargeChange="5"
                                mah:SliderHelper.ChangeValueBy="LargeChange"
                                mah:SliderHelper.EnableMouseWheel="MouseHover"

                                Thumb.DragCompleted="InterfaceScaleSlider_DragCompleted"
                                ValueChanged="InterfaceScaleSlider_ValueChanged" />
                    </Grid>

                    <!-- Application Behavior Section -->
                    <Separator Grid.Row="5" Background="{StaticResource MahApps.Brushes.Gray.SemiTransparent}" Margin="0,10,0,20" />
                    <TextBlock Grid.Row="6" Text="{lex:Loc AppSettings_Section_ApplicationBehavior}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15" />

                    <!-- Start With Windows Toggle -->
                    <Grid Grid.Row="7" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                            <TextBlock Text="{lex:Loc AppSettings_Lbl_StartWithWindows}" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                            <TextBlock Text="{lex:Loc AppSettings_Desc_StartWithWindows}" Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,150,0" />
                        </StackPanel>
                        <mah:ToggleSwitch Grid.Column="1" x:Name="StartWithWindowsToggle"
                                          OnContent="" OffContent=""
                                          Margin="0,0,-100,0"
                                          HorizontalAlignment="Right"
                                          Toggled="StartWithWindowsToggle_Toggled"
                                          Style="{StaticResource MahApps.Styles.ToggleSwitch}"
                                          VerticalAlignment="Center" />
                    </Grid>

                    <!-- Always On Top Toggle -->
                    <Grid Grid.Row="8" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                            <TextBlock Text="{lex:Loc AppSettings_Lbl_AlwaysOnTop}" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                            <TextBlock Text="{lex:Loc AppSettings_Desc_AlwaysOnTop}" Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,150,0" />
                        </StackPanel>
                        <mah:ToggleSwitch Grid.Column="1" x:Name="AlwaysOnTopToggle"
                                          OnContent="" OffContent=""
                                          Margin="0,0,-100,0"
                                          HorizontalAlignment="Right"
                                          Toggled="AlwaysOnTopToggle_Toggled"
                                          Style="{StaticResource MahApps.Styles.ToggleSwitch}"
                                          VerticalAlignment="Center" />
                    </Grid>

                    <!-- Minimize To Tray Toggle -->
                    <Grid Grid.Row="9" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                            <TextBlock Text="{lex:Loc AppSettings_Lbl_MinimizeToTray}" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                            <TextBlock Text="{lex:Loc AppSettings_Desc_MinimizeToTray}" Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,150,0" />
                        </StackPanel>
                        <mah:ToggleSwitch Grid.Column="1" x:Name="MinimizeToTrayToggle"
                                          OnContent="" OffContent=""
                                          Margin="0,0,-100,0"
                                          HorizontalAlignment="Right"
                                          Toggled="MinimizeToTrayToggle_Toggled"
                                          Style="{StaticResource MahApps.Styles.ToggleSwitch}"
                                          VerticalAlignment="Center" />
                    </Grid>

                    <!-- Updates Section -->
                    <Separator Grid.Row="10" Background="{StaticResource MahApps.Brushes.Gray.SemiTransparent}" Margin="0,10,0,20" />
                    <TextBlock Grid.Row="11" Text="{lex:Loc AppSettings_Section_Updates}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15" />

                    <!-- Check for Updates on Startup -->
                    <Grid Grid.Row="12" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                            <TextBlock Text="{lex:Loc AppSettings_Lbl_CheckForUpdatesOnStartup}" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                            <TextBlock Text="{lex:Loc AppSettings_Desc_CheckForUpdatesOnStartup}" Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,150,0" />
                        </StackPanel>
                        <mah:ToggleSwitch Grid.Column="1" x:Name="CheckAppUpdatesToggle"
                                          OnContent="" OffContent=""
                                          Margin="0,0,-100,0"
                                          HorizontalAlignment="Right"
                                          Toggled="CheckAppUpdatesToggle_Toggled"
                                          Style="{StaticResource MahApps.Styles.ToggleSwitch}"
                                          VerticalAlignment="Center" />
                    </Grid>

                    <!-- Check for Updates Button -->
                    <Grid Grid.Row="13" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                            <TextBlock Text="{lex:Loc AppSettings_Lbl_CheckForUpdates}" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                            <TextBlock Text="{lex:Loc AppSettings_Desc_CheckForUpdates}" Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,200,0" />
                        </StackPanel>

                        <Button x:Name="CheckAppUpdatesButton"
                                Width="{lex:Loc Num_AppSettings_Btn_CheckForUpdates_Width}"
                                Height="35"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0 0 0 0"
                                BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
                                Background="Transparent"
                                Click="CheckForUpdates_Click">

                            <Grid HorizontalAlignment="Stretch" Width="{Binding Path=ActualWidth, RelativeSource={RelativeSource AncestorType=Button}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <iconPacks:PackIconMaterial Kind="Update"
                                                            Foreground="{DynamicResource MahApps.Brushes.Gray5}"
                                                            Width="18"
                                                            Height="18"
                                                            VerticalAlignment="Center"
                                                            Margin="10,0,0,0"
                                                            Grid.Column="0" />

                                <TextBlock Text="{lex:Loc AppSettings_Btn_CheckForUpdates}"
                                           Foreground="{DynamicResource MahApps.Brushes.Gray6}"
                                           FontWeight="SemiBold"
                                           FontSize="12"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Grid.Column="1"
                                           Margin="0,0,18,0" />
                            </Grid>
                        </Button>
                    </Grid>

                    <!-- Logging Section -->
                    <Separator Grid.Row="14" Background="{StaticResource MahApps.Brushes.Gray.SemiTransparent}" Margin="0,10,0,20" />
                    <TextBlock Grid.Row="15" Text="{lex:Loc AppSettings_Section_Logging}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15" />

                    <!-- Enable Debug Logging Toggle -->
                    <Grid Grid.Row="16" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                            <TextBlock Text="{lex:Loc AppSettings_Lbl_EnableDebugLogging}" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                            <TextBlock Text="{lex:Loc AppSettings_Desc_EnableDebugLogging}" Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,150,0" />
                        </StackPanel>
                        <mah:ToggleSwitch Grid.Column="1" x:Name="EnableDebugLoggingToggle"
                                          OnContent="" OffContent=""
                                          Margin="0,0,-100,0"
                                          HorizontalAlignment="Right"
                                          Toggled="EnableDebugLoggingToggle_Toggled"
                                          Style="{StaticResource MahApps.Styles.ToggleSwitch}"
                                          VerticalAlignment="Center" />
                    </Grid>

                    <!-- Open Logs Folder Button -->
                    <Grid Grid.Row="17" Margin="0,20,0,20">
                        <Button x:Name="OpenLogsFolderButton"
                                Width="Auto"
                                Height="35"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="20 0 20 0"
                                BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
                                Background="Transparent"
                                Click="OpenLogsFolder_Click">

                            <Grid HorizontalAlignment="Stretch" Width="{Binding Path=ActualWidth, RelativeSource={RelativeSource AncestorType=Button}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <iconPacks:PackIconMaterial Kind="FolderOpen"
                                                            Foreground="{DynamicResource MahApps.Brushes.Gray5}"
                                                            Width="18"
                                                            Height="18"
                                                            VerticalAlignment="Center"
                                                            Margin="10,0,0,0"
                                                            Grid.Column="0" />

                                <TextBlock Text="{lex:Loc AppSettings_Btn_OpenLogsFolder}"
                                           Foreground="{DynamicResource MahApps.Brushes.Gray6}"
                                           FontWeight="SemiBold"
                                           FontSize="12"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Grid.Column="1"
                                           Margin="0,0,18,0" />
                            </Grid>
                        </Button>
                    </Grid>

                    <!-- Maintenance Section -->
                    <Separator Grid.Row="18" Background="{StaticResource MahApps.Brushes.Gray.SemiTransparent}" Margin="0,10,0,20" />
                    <TextBlock Grid.Row="19" Text="{lex:Loc AppSettings_Section_Maintenance}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15" />

                    <!-- Reset To Factory Settings Button -->
                    <Grid Grid.Row="20" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                            <TextBlock Text="{lex:Loc AppSettings_Lbl_ResetToFactorySettings}" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                            <TextBlock Text="{lex:Loc AppSettings_Desc_ResetToFactorySettings}" Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,200,0" />
                        </StackPanel>

                        <Button x:Name="ResetToFactorySettingsButton"
                                Width="{lex:Loc Num_AppSettings_Btn_ResetToFactorySettings_Width}"
                                Height="35"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0 0 0 0"
                                BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
                                Background="Transparent"
                                Click="ResetToFactorySettings_Click">

                            <Grid HorizontalAlignment="Stretch" Width="{Binding Path=ActualWidth, RelativeSource={RelativeSource AncestorType=Button}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <iconPacks:PackIconMaterial Kind="DeleteSweep"
                                                            Foreground="{DynamicResource MahApps.Brushes.Gray5}"
                                                            Width="18"
                                                            Height="18"
                                                            VerticalAlignment="Center"
                                                            Margin="10,0,0,0"
                                                            Grid.Column="0" />

                                <TextBlock Text="{lex:Loc AppSettings_Btn_ResetSettings}"
                                           Foreground="{DynamicResource MahApps.Brushes.Gray6}"
                                           FontWeight="SemiBold"
                                           FontSize="12"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Grid.Column="1"
                                           Margin="0,0,18,0" />
                            </Grid>
                        </Button>
                    </Grid>

                    <!-- Reset Window Position Button -->
                    <Grid Grid.Row="21" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,50,0">
                            <TextBlock Text="{lex:Loc AppSettings_Lbl_ResetWindowPosition}" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="14" />
                            <TextBlock Text="{lex:Loc AppSettings_Desc_ResetWindowPosition}" Foreground="{DynamicResource MahApps.Brushes.Gray7}" FontSize="12" TextWrapping="Wrap" Padding="0,0,200,0" />
                        </StackPanel>

                        <Button x:Name="ResetWindowPositionButton"
                                Width="180"
                                Height="35"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0 0 0 0"
                                BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
                                Background="Transparent"
                                Click="ResetWindowPosition_Click">

                            <Grid HorizontalAlignment="Stretch" Width="{Binding Path=ActualWidth, RelativeSource={RelativeSource AncestorType=Button}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <iconPacks:PackIconMaterial Kind="DeleteSweep"
                                                            Foreground="{DynamicResource MahApps.Brushes.Gray5}"
                                                            Width="18"
                                                            Height="18"
                                                            VerticalAlignment="Center"
                                                            Margin="10,0,0,0"
                                                            Grid.Column="0" />

                                <TextBlock Text="{lex:Loc AppSettings_Btn_ResetPosition}"
                                           Foreground="{DynamicResource MahApps.Brushes.Gray6}"
                                           FontWeight="SemiBold"
                                           FontSize="12"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Grid.Column="1"
                                           Margin="0,0,18,0" />
                            </Grid>
                        </Button>
                    </Grid>

                    <!-- Open App Config Folder Button -->
                    <Grid Grid.Row="22" Margin="0,20,0,20">
                        <Button x:Name="OpenAppConfigFolderButton"
                                Width="Auto"
                                Height="35"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="20 0 20 0"
                                BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
                                Background="Transparent"
                                Click="OpenAppConfigFolder_Click">

                            <Grid HorizontalAlignment="Stretch" Width="{Binding Path=ActualWidth, RelativeSource={RelativeSource AncestorType=Button}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <iconPacks:PackIconMaterial Kind="FolderOpen"
                                                            Foreground="{DynamicResource MahApps.Brushes.Gray5}"
                                                            Width="18"
                                                            Height="18"
                                                            VerticalAlignment="Center"
                                                            Margin="10,0,0,0"
                                                            Grid.Column="0" />

                                <TextBlock Text="{lex:Loc AppSettings_Btn_OpenAppConfigFolder}"
                                           Foreground="{DynamicResource MahApps.Brushes.Gray6}"
                                           FontWeight="SemiBold"
                                           FontSize="12"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Grid.Column="1"
                                           Margin="0,0,18,0" />
                            </Grid>
                        </Button>
                    </Grid>
                </Grid>
            </ScrollViewer>

            <!-- App Settings Flyout Footer -->
            <Border Grid.Row="1" Background="{StaticResource Alr.Brushes.Flyout.Footer.Background}" BorderBrush="{StaticResource MahApps.Brushes.Gray.SemiTransparent}" BorderThickness="0 1 0 0" Padding="15">
                <TextBlock Grid.Row="1"
                           Foreground="{DynamicResource MahApps.Brushes.Gray2}"
                           FontSize="12"
                           TextAlignment="Center"
                           Margin="0,0,0,0"
                           Text="{lex:Loc AppSettings_Footer_YourChangesAreSavedAutomatically}" />
            </Border>
        </Grid>
    </mah:Flyout>
    <!-- ============ App Settings Flyout END ============ -->

    <!-- ============ About Flyout START ============ -->
    <mah:Flyout x:Name="AboutFlyout"
            Header="{lex:Loc About_Title}"
            Position="Left"
            Width="{lex:Loc Num_About_Flyout_Width}"
            Foreground="{DynamicResource MahApps.Brushes.Flyout.Foreground}"
            Background="{DynamicResource Alr.Brushes.Flyout.Background}"
            BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
            BorderThickness="0 0 1 0"
            IsOpen="{Binding IsAboutFlyoutOpen}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <Grid Margin="25,10,25,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Separator Grid.Row="0" Background="{StaticResource MahApps.Brushes.Gray.SemiTransparent}" Margin="0,15,0,15" />

                    <!-- ================ Support START ================ -->
                    <StackPanel Grid.Row="1" Margin="0,5,0,0">
                        <StackPanel.Visibility>
                            <MultiBinding Converter="{StaticResource AllStringsNullOrEmptyToVisibilityConverter}">
                                <Binding Source="{x:Static constants:AppUrls.BuyMeACoffeeUrl}" />
                                <Binding Source="{x:Static constants:AppUrls.PatreonUrl}" />
                            </MultiBinding>
                        </StackPanel.Visibility>

                        <TextBlock Text="{lex:Loc About_Section_SupportTheDeveloper}" HorizontalAlignment="Center" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,20" />

                        <TextBlock Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,40" Padding="20,0,20,0"
                               Text="{lex:Loc About_Section_SupportTheDeveloper_Desc}" />

                        <StackPanel Orientation="Vertical">
                            <Button x:Name="BuyMeACoffeeButton" Visibility="{Binding Source={x:Static constants:AppUrls.BuyMeACoffeeUrl}, Converter={StaticResource StringToVisibilityConverter}}" Width="180" Height="35" Margin="0,0,0,12" HorizontalAlignment="Center" BorderBrush="{DynamicResource MahApps.Brushes.Gray7}" Background="Transparent" Click="OpenBuyMeACoffee_Click">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <iconPacks:PackIconSimpleIcons Kind="BuyMeACoffee" Foreground="{DynamicResource MahApps.Brushes.Gray5}" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <TextBlock Text="Buy Me a Coffee" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>

                            <Button x:Name="PatreonButton" Visibility="{Binding Source={x:Static constants:AppUrls.PatreonUrl}, Converter={StaticResource StringToVisibilityConverter}}" Width="180" Height="35" Margin="0,0,0,5" HorizontalAlignment="Center" BorderBrush="{DynamicResource MahApps.Brushes.Gray7}" Background="Transparent" Click="OpenPatreon_Click">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <iconPacks:PackIconSimpleIcons Kind="Patreon" Foreground="{DynamicResource MahApps.Brushes.Gray5}" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <TextBlock Text="Patreon" Foreground="{DynamicResource MahApps.Brushes.Gray6}" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                    <!-- ================ Support END ================ -->

                    <Separator Grid.Row="2" Background="{StaticResource MahApps.Brushes.Gray.SemiTransparent}" Margin="0,15,0,15" />

                    <!-- ================ Application Information START ================ -->
                    <StackPanel Grid.Row="3" Margin="0,5,0,10">
                        <TextBlock Text="{lex:Loc About_Section_ApplicationInformation}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource MahApps.Brushes.Accent}" Margin="0,0,0,20" TextAlignment="Center" />

                        <UniformGrid Columns="2" Margin="0,0,0,15">
                            <StackPanel Margin="5,0" HorizontalAlignment="Center">
                                <TextBlock Text="{lex:Loc About_Lbl_Name}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource MahApps.Brushes.Gray4}" Margin="0,0,0,2" TextAlignment="Center" />
                                <TextBlock Text="{x:Static constants:AppConfig.DisplayAppName}" FontSize="13" FontWeight="SemiBold" TextAlignment="Center" />
                            </StackPanel>
                            <StackPanel Margin="5,0" HorizontalAlignment="Center">
                                <TextBlock Text="{lex:Loc About_Lbl_Version}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource MahApps.Brushes.Gray4}" Margin="0,0,0,2" TextAlignment="Center" />
                                <TextBlock Text="{x:Static constants:AppConfig.DisplayAppVersion}" FontSize="13" FontWeight="SemiBold" TextAlignment="Center" />
                            </StackPanel>
                        </UniformGrid>

                        <UniformGrid Columns="2" Margin="0,0,0,15">
                            <StackPanel Margin="5,0" HorizontalAlignment="Center">
                                <TextBlock Text="{lex:Loc About_Lbl_Developer}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource MahApps.Brushes.Gray4}" Margin="0,0,0,2" TextAlignment="Center" />
                                <TextBlock Text="{x:Static constants:AppConfig.AppAuthor}" FontSize="13" FontWeight="SemiBold" TextAlignment="Center" />
                            </StackPanel>
                            <StackPanel Margin="5,0" HorizontalAlignment="Center">
                                <TextBlock Text="{lex:Loc About_Lbl_License}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource MahApps.Brushes.Gray4}" Margin="0,0,0,2" TextAlignment="Center" />
                                <TextBlock FontSize="13" FontWeight="SemiBold" TextAlignment="Center">
                                    <Hyperlink RequestNavigate="Hyperlink_RequestNavigate"
                                               NavigateUri="{x:Static constants:AppUrls.AppLicenseUri}">

                                        <Hyperlink.Style>
                                            <Style TargetType="Hyperlink">
                                                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Flyout.Foreground}" />
                                                <Style.Triggers>
                                                    <Trigger Property="NavigateUri" Value="{x:Null}">
                                                        <Setter Property="IsEnabled" Value="False" />
                                                        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Flyout.Foreground}" />
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Accent}" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Hyperlink.Style>

                                        <Run>
                                            <Run.Text>
                                                <Binding Source="{x:Static constants:AppConfig.AppLicense}" Mode="OneWay" StringFormat="{}{0}" />
                                            </Run.Text>
                                        </Run>
                                    </Hyperlink>
                                </TextBlock>
                            </StackPanel>
                        </UniformGrid>

                        <UniformGrid Columns="2" Margin="0,0,0,5">
                            <StackPanel Margin="5,0" HorizontalAlignment="Center">
                                <TextBlock Text="{lex:Loc About_Lbl_TargetFramework}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource MahApps.Brushes.Gray4}" Margin="0,0,0,2" TextAlignment="Center" />
                                <TextBlock Text="{x:Static constants:AppConfig.TargetFramework}" FontSize="13" FontWeight="SemiBold" TextAlignment="Center" />
                            </StackPanel>
                            <StackPanel Margin="5,0" HorizontalAlignment="Center">
                                <TextBlock Text="{lex:Loc About_Lbl_OsArchitecture}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource MahApps.Brushes.Gray4}" Margin="0,0,0,2" TextAlignment="Center" />
                                <TextBlock Text="{x:Static constants:AppConfig.OsArchitecture}" FontSize="13" FontWeight="SemiBold" TextAlignment="Center" />
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                    <!-- ================ Application Information END ================ -->
                </Grid>
            </ScrollViewer>
        </Grid>
    </mah:Flyout>
    <!-- ============ About Flyout END ============ -->

    <!-- IoStore Flyout -->
    <mah:Flyout x:Name="IoStoreFlyout"
                        Header="IoStore Packing"
                        Position="Left"
                        Width="1271"
                        Background="#1F1F1F"
                        BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                        BorderThickness="1"
                        IsOpen="{Binding IsIoStoreFlyoutOpen}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="600" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left Column -->
            <StackPanel Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="5">
                <!-- UnrealPak Path -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="UnrealPak Path (requires Unreal Engine 4.25+)" Foreground="White" FontSize="16" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="5" />
                    <Grid Margin="5">
                        <TextBox Name="UnrealPakPathTextBox" IsReadOnly="True" Background="Transparent" Width="500" VerticalContentAlignment="Center" mah:TextBoxHelper.Watermark="C:\Program Files\Epic Games\UE_5.1\Engine\Binaries\Win64\UnrealPak.exe" />
                        <Button Content="Browse" Background="#262626" Width="75" VerticalAlignment="Center" HorizontalAlignment="Right" Click="BrowseUnrealPakPath" />
                    </Grid>
                </StackPanel>

                <!-- Output Path -->
                <StackPanel Margin="0,15,0,15">
                    <TextBlock Text="Output Path (for global.utoc)" Foreground="White" FontSize="16" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="5" />
                    <Grid Margin="5">
                        <TextBox Name="GlobalOutputPathTextBox" IsReadOnly="True" Background="Transparent" Width="500" VerticalContentAlignment="Center" mah:TextBoxHelper.Watermark="C:\Path\To\Output" />
                        <Button Content="Browse" Background="#262626" Width="75" VerticalAlignment="Center" HorizontalAlignment="Right" Click="BrowseGlobalOutputPath" />
                    </Grid>
                </StackPanel>

                <!-- Input Path -->
                <StackPanel Margin="0,15,0,15">
                    <TextBlock Text="Cooked Files Path (cooked files to be packaged)" Foreground="White" FontSize="16" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="5" />
                    <Grid Margin="5">
                        <TextBox Name="CookedFilesPathTextBox" IsReadOnly="True" Background="Transparent" Width="500" VerticalContentAlignment="Center" mah:TextBoxHelper.Watermark="C:\Path\To\Cooked" />
                        <Button Content="Browse" Background="#262626" Width="75" VerticalAlignment="Center" HorizontalAlignment="Right" Click="BrowseCookedFilesPath" />
                    </Grid>
                </StackPanel>

                <!-- Packagestore.manifest Path -->
                <StackPanel Margin="0,15,0,15">
                    <TextBlock Text="PackageStore Path" Foreground="White" FontSize="16" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="5" />
                    <Grid Margin="5">
                        <TextBox Name="PackageStorePathTextBox" IsReadOnly="True" Background="Transparent" Width="500" VerticalContentAlignment="Center" mah:TextBoxHelper.Watermark="packagestore.manifest / PackageStoreManifest.json" />
                        <Button Content="Browse" Background="#262626" Width="75" VerticalAlignment="Center" HorizontalAlignment="Right" Click="BrowsePackageStorePath" />
                    </Grid>
                </StackPanel>

                <!-- ScriptObjects.bin Path -->
                <StackPanel Margin="0,15,0,15">
                    <TextBlock Text="ScriptObjects Path" Foreground="White" FontSize="16" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="5" />
                    <Grid Margin="5">
                        <TextBox Name="ScriptObjectsPathTextBox" IsReadOnly="True" Background="Transparent" Width="500" VerticalContentAlignment="Center" mah:TextBoxHelper.Watermark="ScriptObjects.bin" />
                        <Button Content="Browse" Background="#262626" Width="75" VerticalAlignment="Center" HorizontalAlignment="Right" Click="BrowseScriptObjectsPath" />
                    </Grid>
                </StackPanel>

                <!-- IoStoreCommands.txt Path -->
                <StackPanel Margin="0,15,0,15">
                    <TextBlock Text="Commands Path" Foreground="White" FontSize="16" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="5" />
                    <Grid Margin="5">
                        <TextBox Name="IoStoreCommandsPathTextBox" IsReadOnly="True" Background="Transparent" Width="500" VerticalContentAlignment="Center" mah:TextBoxHelper.Watermark="IoStoreCommands.txt / Commands.txt" />
                        <Button Content="Browse" Background="#262626" Width="75" VerticalAlignment="Center" HorizontalAlignment="Right" Click="BrowseIoStoreCommandsPath" />
                    </Grid>
                </StackPanel>

                <!-- Crypto Keys Browser -->
                <StackPanel Margin="0,50,0,0">
                    <Button Content="Edit Crypto.json" Background="#262626" Width="200" Height="40" FontSize="12" FontWeight="Bold" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Center" Click="OpenCryptoKeysFileAsync" />
                </StackPanel>
            </StackPanel>

            <!-- Right Column -->
            <Grid Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="200" />
                    <RowDefinition Height="200" />
                    <RowDefinition Height="260" />
                </Grid.RowDefinitions>

                <!-- Config Selection -->
                <!--
                        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0">
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Button Background="#262626" Height="30" Width="30" FontSize="12" FontWeight="Bold" ToolTip="Add New Config" Grid.Column="0">
                                    <Button.Content>
                                        <iconPacks:PackIconMaterial Kind="PlusBox" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Button.Content>
                                </Button>

                                <Button Background="#262626" Height="30" Width="30" FontSize="12" FontWeight="Bold" ToolTip="Delete Selected Config" Grid.Column="1">
                                    <Button.Content>
                                        <iconPacks:PackIconMaterial Kind="MinusBox" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Button.Content>
                                </Button>

                                <Button Background="#262626" Height="30" Width="30" FontSize="12" FontWeight="Bold" ToolTip="Rename Selected Config" Grid.Column="2">
                                    <Button.Content>
                                        <iconPacks:PackIconMaterial Kind="RenameBox" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Button.Content>
                                </Button>

                                <ComboBox Name="ConfigSelectionComboBox"
                                          Background="Transparent"
                                          Height="30"
                                          Width="200"
                                          VerticalContentAlignment="Center"
                                          Margin="5,0,0,0"
                                          Grid.Column="3"
                                          ItemsSource="{Binding Configurations}"
                                          SelectedItem="{Binding SelectedConfiguration, Mode=TwoWay}"
                                          SelectionChanged="ConfigSelectionComboBox_SelectionChanged">
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                        -->

                <!-- Pack Button -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
                    <Button Content="Pack" Background="#262626" Width="200" Height="40" FontSize="14" FontWeight="Bold" Margin="5"  />
                </StackPanel>

                <!-- CLI Output -->
                <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Background="#262626" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,0">
                    <!-- TextBox to display CLI output -->
                    <TextBox x:Name="IoStoreCommandOutputTextBox"
                                     Background="#202020"
                                     Foreground="White"
                                     FontSize="12"
                                     IsReadOnly="True"
                                     VerticalScrollBarVisibility="Auto"
                                     Height="250"
                                     Width="650"
                                     BorderBrush="#7F7F7F"
                                     BorderThickness="1"
                                     FocusVisualStyle="{x:Null}"
                                     TextChanged="CliOutput_TextChanged">
                        <TextBox.Template>
                            <ControlTemplate TargetType="TextBox">
                                <Border BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Background="{TemplateBinding Background}">
                                    <ScrollViewer x:Name="PART_ContentHost" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
                                </Border>
                            </ControlTemplate>
                        </TextBox.Template>
                    </TextBox>
                </StackPanel>
            </Grid>
        </Grid>
    </mah:Flyout>

    <!-- Aes Keys Flyout -->
    <mah:Flyout x:Name="AesKeysFlyout"
                        Header="AES Key Settings"
                        Position="Left"
                        Width="600"
                        Background="#1F1F1F"
                        BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                        BorderThickness="1"
                        IsOpen="{Binding IsAesKeysFlyoutOpen}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="250" />
                <RowDefinition Height="*" />
                <RowDefinition Height="250" />
            </Grid.RowDefinitions>

            <!-- Repak AES Key Input Section -->
            <StackPanel Grid.Row="0" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="5">
                <TextBlock Text="Repak AES Key" Foreground="White" FontSize="20" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="5" />
                <TextBox Name="AesKeyTextBox" Background="Transparent" Width="460" Height="30" Margin="5" />
                <Button Content="Save" Background="#262626" FontSize="14" FontWeight="Bold" Width="180" Height="40" Margin="5" Click="SaveRepakConfigAsync" />
            </StackPanel>

            <!-- Empty Row (Filler) -->
            <StackPanel Grid.Row="1" Visibility="Collapsed" />

            <!-- ZenTools AES Key Input Section -->
            <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="5">
                <TextBlock Text="ZenTools AES Key" Foreground="White" FontSize="20" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="5" />
                <TextBlock Text="GUID" Foreground="White" FontSize="12" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="5" />
                <TextBox Name="ZenToolsKeyGuidTextBox" Background="Transparent" Width="460" Height="30" Margin="5" />
                <TextBlock Text="HEX" Foreground="White" FontSize="12" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="5" />
                <TextBox Name="ZenToolsKeyHexTextBox" Background="Transparent" Width="460" Height="30" Margin="5" />
                <Button Content="Save" Background="#262626" FontSize="14" FontWeight="Bold" Width="180" Height="40" Margin="5" Click="SaveZenToolsConfigAsync" />
            </StackPanel>
        </Grid>
    </mah:Flyout>

    <!-- Repak Settings Flyout -->
    <mah:Flyout x:Name="RepakSettingsFlyout"
                        Header="Repak Settings"
                        Position="Left"
                        Width="600"
                        Background="#1F1F1F"
                        BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                        BorderThickness="1"
                        IsOpen="{Binding IsRepakSettingsFlyoutOpen}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="250" />
                <RowDefinition Height="*" />
                <RowDefinition Height="350" />
            </Grid.RowDefinitions>

            <!-- Top -->
            <StackPanel Grid.Row="0" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="5">
                <!-- Dropdown for Version Switching -->
                <TextBlock Text="Pak Version" Foreground="White" FontSize="20" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center" Margin="0,5,0,15" />
                <ComboBox x:Name="RepakVersionSwitchDropdown"
                                  Width="150" Height="40"
                                  SelectedIndex="0"
                                  SelectionChanged="ComboBox_RepakVersion"
                                  FontSize="16" FontWeight="Bold"
                                  VerticalAlignment="Center" HorizontalAlignment="Center"
                                  Grid.Column="1">
                    <ComboBoxItem Content="V11" FontSize="14" Height="25" />
                    <ComboBoxItem Content="V10" FontSize="14" Height="25" />
                    <ComboBoxItem Content="V9" FontSize="14" Height="25" />
                    <ComboBoxItem Content="V8B" FontSize="14" Height="25" />
                    <ComboBoxItem Content="V8A" FontSize="14" Height="25" />
                    <ComboBoxItem Content="V7" FontSize="14" Height="25" />
                    <ComboBoxItem Content="V6" FontSize="14" Height="25" />
                    <ComboBoxItem Content="V5" FontSize="14" Height="25" />
                    <ComboBoxItem Content="V4" FontSize="14" Height="25" />
                    <ComboBoxItem Content="V3" FontSize="14" Height="25" />
                    <ComboBoxItem Content="V2" FontSize="14" Height="25" />
                    <ComboBoxItem Content="V1" FontSize="14" Height="25" />
                </ComboBox>
            </StackPanel>

            <!-- Middle -->
            <StackPanel Grid.Row="1" Visibility="Collapsed" />

            <!-- Bottom -->
            <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="5">
                <DataGrid x:Name="RepakDataGrid"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="None"
                                  CanUserSortColumns="False"
                                  RowBackground="#202020"
                                  AlternatingRowBackground="#252525"
                                  Foreground="White"
                                  Background="#262626"
                                  BorderBrush="#7F7F7F"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Width="580">

                    <!-- Columns -->
                    <DataGrid.Columns>
                        <!-- UE Version Column -->
                        <DataGridTextColumn Header="UE Version" Binding="{Binding UEVersion}" Width="1*" ElementStyle="{StaticResource CenteredCellStyle}">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="Height" Value="30" />
                                    <Setter Property="FontSize" Value="14" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="Background" Value="#2A2A2A" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Setter Property="BorderBrush" Value="#B9B9B9" />
                                    <Setter Property="BorderThickness" Value="0,0,2,3" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>

                        <!-- Version Column -->
                        <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="0.5*" ElementStyle="{StaticResource CenteredCellStyle}">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="Height" Value="30" />
                                    <Setter Property="FontSize" Value="14" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="Background" Value="#2A2A2A" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Setter Property="BorderBrush" Value="#B9B9B9" />
                                    <Setter Property="BorderThickness" Value="0,0,2,3" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>

                        <!-- Version Feature Column -->
                        <DataGridTextColumn Header="Version Feature" Binding="{Binding VersionFeature}" Width="1.5*" ElementStyle="{StaticResource CenteredCellStyle}">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="Height" Value="30" />
                                    <Setter Property="FontSize" Value="14" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="Background" Value="#2A2A2A" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Setter Property="BorderBrush" Value="#B9B9B9" />
                                    <Setter Property="BorderThickness" Value="0,0,2,3" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>

                        <!-- Read Column -->
                        <DataGridTemplateColumn Header="Read" Width="0.5*">
                            <DataGridTemplateColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="Height" Value="30" />
                                    <Setter Property="FontSize" Value="14" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="Background" Value="#2A2A2A" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Setter Property="BorderBrush" Value="#B9B9B9" />
                                    <Setter Property="BorderThickness" Value="0,0,2,3" />
                                </Style>
                            </DataGridTemplateColumn.HeaderStyle>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Read}" HorizontalAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Write Column -->
                        <DataGridTemplateColumn Header="Write" Width="0.5*">
                            <DataGridTemplateColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="Height" Value="30" />
                                    <Setter Property="FontSize" Value="14" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="Background" Value="#2A2A2A" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Setter Property="BorderBrush" Value="#B9B9B9" />
                                    <Setter Property="BorderThickness" Value="0,0,0,3" />
                                </Style>
                            </DataGridTemplateColumn.HeaderStyle>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Write}" HorizontalAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Grid>
    </mah:Flyout>
</mah:FlyoutsControl>