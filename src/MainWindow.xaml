<mah:MetroWindow x:Class="PakMaster.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PakMaster"
        Title="PakMaster"
        Width="1200"
        Height="675"
        GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d">

    <mah:MetroWindow.LeftWindowCommands>
        <mah:WindowCommands>
            <Button Click="LaunchBrowserGitHubPakMaster" ToolTip="Open up the GitHub site">
                <iconPacks:PackIconModern Width="22"
                                  Height="22"
                                  Kind="SocialGithubOctocat" />
            </Button>
        </mah:WindowCommands>
    </mah:MetroWindow.LeftWindowCommands>

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <Button Click="CheckForUpdatesPakMaster" Content="Check for updates">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconModern Width="22"
                                        Height="22"
                                        VerticalAlignment="Center"
                                        Kind="Refresh" />
                            <TextBlock Margin="4 0 0 0"
                         VerticalAlignment="Center"
                         Text="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <Grid Background="#2B2B2B">
        <!-- Define the main layout grid -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Input Section -->
        <StackPanel Grid.Column="0" Background="#262626" Margin="10">
            <TextBlock Text="Input" Foreground="White" FontSize="24" HorizontalAlignment="Center" />
            <!-- ListBox to display files -->
            <ListBox Name="InputFilesListBox" Background="Transparent" Margin="5" Height="400" />
            <Button Content="Browse input folder" Margin="5" Click="BrowseInputFolder" />
        </StackPanel>

        <!-- Center Buttons -->
        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10">
            <Button Content="Unpack" Background="DarkRed" Foreground="White" Margin="5" />
            <Button Content="Repack" Background="DarkRed" Foreground="White" Margin="5" />
        </StackPanel>

        <!-- Output Section -->
        <StackPanel Grid.Column="2" Background="#262626" Margin="10">
            <TextBlock Text="Output" Foreground="White" FontSize="24" HorizontalAlignment="Center" />
            <!-- ListBox to display output files -->
            <ListBox Name="OutputFilesListBox" Background="Transparent" Margin="5" Height="400" />
            <Button Content="Browse output folder" Margin="5" Click="BrowseOutputFolder" />
        </StackPanel>

        <!-- Command Line Output -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Background="#262626" Margin="10" Height="125">
            <TextBlock Text="Command line / Terminal Output/Display" Foreground="White" FontSize="12" HorizontalAlignment="Center" Margin="5" />
        </StackPanel>
    </Grid>
</mah:MetroWindow>
